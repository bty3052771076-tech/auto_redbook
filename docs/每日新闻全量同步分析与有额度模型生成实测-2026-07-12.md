# 每日新闻全量同步、选题分析与有额度模型生成实测（2026-07-12）

## 全量同步

使用已登录的小红书创作者中心 profile 执行：

```powershell
.\.venv\Scripts\python.exe -m apps.cli update-metrics --limit 0 --headless --login-hold 0 --wait-timeout 600
```

结果：`target=368`、`fetched=368`、`missing=0`、`complete=True`。最新指标快照已写入 `data/analytics/published_metrics_latest.csv`，并同步到本地帖子记录。

## 今日方向分析

368 条帖子累计浏览 13,256，点赞/评论/收藏为 56/83/26。历史高互动方向集中在争议文化/体育/品牌，其次是社会民生、国际冲突/外交安全；财经产业与硬科技适合保留少量测试位，标题应突出具体主体、动作和影响。

## 有额度模型生成

本轮使用阿里云百炼：

- LLM：`glm-5.2`，快照剩余约 177,354 token。
- 生图：`qwen-image-2.0-pro-2026-06-22`，快照剩余 44 次。
- 火山引擎本轮没有返回可核验的免费额度，因此没有强行使用火山模型。

候选池按 10 条目标请求了约 20 倍候选，并使用三日窗口、相关性、来源多样性和国内新闻配额筛选。最终三日窗口内得到 22 条候选，其中 20 条因国内新闻配额保留规则被跳过，实际生成并上传 2 条：

1. `稳定币巨头Circle获信托银行牌照`
2. `比亚迪携三大品牌亮相古德伍德速度节`

两条均已完成 AI 生图、草稿校验和小红书创作者中心草稿箱验证，状态为 `saved_as_draft`。没有用旧日期新闻补足数量。
