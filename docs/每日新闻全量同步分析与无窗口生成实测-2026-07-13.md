# 每日新闻全量同步、分析与无窗口生成实测（2026-07-13）

## 同步

使用 `update-metrics --limit 0 --headless` 完成全量同步：`368/368`，缺失 `0`，`complete=True`。最新快照已更新至 `data/analytics/published_metrics_latest.csv`。

## 分析

本轮历史表现优先方向为：争议文化/体育/品牌（25%）、社会民生/公共事件（20%）、国际冲突/外交安全（20%）。财经产业、健康科学和硬科技作为补充测试方向。

## 无窗口生成

使用阿里云有额度模型：`glm-5.2` + `qwen-image-2.0-pro-2026-06-22`，命令使用 `--headless`，没有打开可见浏览器窗口。

请求 3 条，最终生成并保存 2 条草稿：

- `长鑫科技IPO倒计时`
- `苹果起诉OpenAI窃密开发AI硬件`

两条均完成 LLM、AI 生图、图片上传、草稿校验和草稿箱回查。第 3 条因正文质量校验失败，另有候选因国内新闻配额被跳过，未使用旧日期新闻补足。
